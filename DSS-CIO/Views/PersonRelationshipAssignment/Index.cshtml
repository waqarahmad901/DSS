@model IEnumerable<DSS_CIO.Models.PersonRelationshipAssignment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Relationship)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.FName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person1.FName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Relationship)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person1.FName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RelationshipID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RelationshipID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RelationshipID })
        </td>
    </tr>
}

</table>
